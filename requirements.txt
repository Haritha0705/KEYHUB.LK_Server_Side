# Web framework + server
fastapi==0.138.0
fastapi-cli==0.0.27          # provides the `fastapi run` command used in Dockerfile
uvicorn[standard]==0.49.0    # ASGI server (pulls uvloop, httptools, websockets, watchfiles)
python-multipart==0.0.32     # form / file upload parsing

# Data validation / settings
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic-extra-types==2.11.1
email-validator==2.3.0       # required for pydantic EmailStr
python-dotenv==1.2.2

# Database (async PostgreSQL)
sqlalchemy==2.0.51
sqlmodel==0.0.38
alembic==1.18.4
asyncpg==0.31.0              # async PostgreSQL driver
